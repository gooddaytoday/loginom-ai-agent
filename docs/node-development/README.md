# Исторический комплект разработки узлов Loginom AI Agent

Статус всего комплекта, включая `workflow/`, `templates/`, реестр, планы узлов
и сведения о памяти: **reference/historical**, снимок 2026-09-24.
Прежние команды, лимиты, назначения, архивирование и результаты приёмки
не являются разрешением или доказательством готовности Loginom Lab.

Новый владелец регламента — `kartamyshev-dev/loginom-swarm`,
`doc/node-development`. [Закреплённый подготовительный вариант][lab-preparation]
находится в `lab-preparation`, имеет статус **NOT_READY** и ещё не принят в `swarm`.
Он описывает целевой процесс; запуск требует подтверждённой готовности
и действующего назначения Paperclip в компании Loginom Lab.

Исторические 78 компонентов и 14 реализованных обработчиков сохраняются
без повышения статусов. Они не доказывают текущую техническую CLI/Desktop
или аналитическую приёмку. Сведения о памяти поколения `20260924.1` ниже
относятся к прежнему окружению и не подтверждают допуск новой попытки Lab;
действующие локальные регистрации и cursors сохраняются.

[lab-preparation]: https://github.com/kartamyshev-dev/loginom-swarm/blob/c4aa3174f9ab79b61924ba2e9f0162e75c786cc2/doc/node-development/README.md

Прежний регламент заменял запуск потоков через Hermes и плагины Codex
из исторического Dock. Разработчик работал в Codex; сценарий при автономной
приёмке строил **standalone CLI Loginom AI Agent**.
Слово «обучение узла» здесь означает разработку и проверку обработчика интерфейса.
Обучение статистической модели внутри Loginom — отдельное свойство конкретного узла.

## Историческая навигация

| Задача | Что передать агенту |
| --- | --- |
| Управлять разрешённой очередью | [Оркестратор](workflow/orchestrator.md), [жизненный цикл](workflow/lifecycle.md), [кампания](templates/campaign.json) |
| Обработать один узел | [Один узел](workflow/single-node.md), карточка/подплан узла, [назначение](templates/assignment.md) |
| Создать подплан нового узла | [Инструкция](workflow/new-node-plan.md), [шаблон](templates/node-plan.md), запись реестра |
| Узнать, что уже работает | [Обзор](inventory.md), [реестр](registry.json), карточки в nodes |
| Провести итоговую проверку | [Приёмка CLI](workflow/acceptance-cli.md), конкретный подплан и независимые ожидаемые результаты |
| Возобновить работу | [Checkpoint](templates/checkpoint.md), последнее подтверждённое событие и актуальный код |
| Подключить память нового worktree | [Текущий механизм регистрации](../../services/loginom-ai/tools/project-memory/CURRENT.md), затем [допуск к разработке](workflow/lifecycle.md) |
| Найти прежние решения | [Исторические материалы](history/README.md), [происхождение](provenance.json) |

## Как читать исторические доказательства

Прежние правила находятся в `workflow/`. Для каждого документированного узла
`nodes/<slug>/README.md` показывает историческую готовность и ограничения, а соседний `plan.md`
содержит предметный подплан. Подпланы готовых режимов служат основой сопровождения;
само наличие документа не назначает повторную разработку. Кросс-таблица имеет
отдельный подплан со статусом исследования. Остальным компонентам подпланы
создаются по мере назначения.

1. Текущее назначение пользователя и правила активного репозитория.
2. Наблюдаемое состояние и код закреплённого candidate.
3. Подтверждения реестра с версиями, платформой и границами проверки.
4. Исторические подпланы и память как справочные материалы.

`registry.json` — источник сводных чисел и исторических состояний готовности в этом
комплекте; `inventory.md` генерируется. Реестр не управляет процессами. В прежнем
процессе очередь, владельцы, события и ресурсы учитывались в приватном состоянии кампании.
Для Loginom Lab это состояние не заменяет текущие назначения и claims Paperclip.
Смена parallel/sequential не меняет готовность обработчика.

В доказательствах `source_commit` означает Git-ревизию, `runtime` — закреплённую
версию или hash проверенного исполнения, `sha256` — hash документа по текущей
ссылке. Для адаптированной истории `source_sha256` закрепляет оригинал из ZIP,
а `adapted_sha256` в provenance — новую навигационную копию. Отсутствующее значение не означает проверку на текущем HEAD: для
исторического документа ревизия могла не сохраниться, а для каталога платформа
не применима. Точные пределы указаны в `scope` и самом источнике.

Каталог компонентов — исторический снимок, а не повторный осмотр всех редакций
Loginom. У нового клиента имеются технические Desktop-проверки; они не объявляются
проверкой аналитической правильности и не заменяют целевую CLI-приёмку.

## Исторические настройки

- База кампании — проверенный commit продуктовой ветки `loginom`, не автоматически текущий HEAD.
- До четырёх разработчиков Astra medium; один итоговый CLI-прогон GPT-6 Sol (`openai/gpt-6-sol`) / low через имеющуюся подписку ChatGPT.
- Одна задача/worktree/ветка на узел. Продолжение только внутри разрешённой очереди;
  single заканчивается на одном узле. Слияние и публикация — отдельно.
- Один этап ревью в задаче разработчика, один раунд исправлений и адресная проверка;
  это не независимое ревью другим агентом.
- Для будущих worktrees установлен [механизм регистрации нового проекта](../../services/loginom-ai/tools/project-memory/CURRENT.md)
  поколения `20260924.1`. **[Полный цикл чтения и записи проверен](workflow/shared-memory.md).**
  Каждый worktree получает отдельный допуск; исходники механизма не заменяют
  [проверку фактического доступа](workflow/lifecycle.md).
- Этот комплект сам по себе **не запускает очередь** и не меняет установленные клиенты.

## Исторические команды проверки и архивирования

Команды ниже сохранены для понимания прежнего комплекта. В Loginom Lab архивы
не создавать и `package.py` не запускать. Проверка документации сама по себе
не подтверждает готовность исполнителей или приёмку узлов.

Из корня `loginom-ai-agent`:

```sh
python3 docs/node-development/tools/validate.py --render
python3 docs/node-development/tools/validate.py
python3 docs/node-development/tools/package.py
```

Последняя команда создаёт новый ZIP, manifest/SHA256 и проверенную распакованную
копию в `~/Backups/loginom-node-development/`; существующие архивы не заменяет.
Неизменённые оригиналы сохраняются в исходном ZIP вне Git; его имя и SHA256 есть
в [provenance.json](provenance.json). Для их повторной проверки передать
`validate.py --source-archive <путь к исходному ZIP>`. История в репозитории
адаптирована для навигации: уже перенесённые материалы переиспользуются,
отличающиеся версии находятся в `history/supplements/`. Исторические JSON
ссылаются на [исходные идентичности](history/references.json), чтобы прежний hash
не приписывался текущему коду. [Неперенесённые материалы](history/unavailable.md)
обозначены явно. Код и внешние доказательства,
не входящие в документационный архив, перечисляются отдельно. Это архив знаний,
а не установочный комплект или резервная копия runtime.

[Проверка комплектности и сценариев регламента](validation.md).
