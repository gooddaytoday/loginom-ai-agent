# Linux archive with Bun JS, SQLite and Node header sources

Version `0.1.4-cli.20260918sources`, dev, Linux x64. Clean source commit
`d391443bec8dd835d1c7f7e6e6a1bc1a757838c7`, source tree SHA256
`0403a955a709e42a0cadbb043029963ecd3d17d75b949539c7344f8bc514a6c5`.

- Payload `/tmp/loginom-linux-bun-sources`.
- Archive `/tmp/loginom-ai-agent-cli-0.1.4-cli.20260918sources-linux-x64.tar.gz`.
- SHA256 `a59fab1f4f075b7ceefecf326e0b16171e52b14dc435ddd0c5b617463d1f180d`.
- Build log `/tmp/loginom-linux-bun-sources-build.log`.
- Project source `/tmp/loginom-ai-agent-cli-0.1.4-cli.20260918sources-source.tar.gz`.
- Source SHA256 `46dfa4411e15536490c1f29db4c547ca083456766017f727528ba3a752ef71e6`.

Build/native version/source stability/complete manifest/extracted archive PASS.
The project source archive exactly matches 11926 git-tree entries.
Temporary user installation: restricted-PATH help/version created no profile;
unconfigured status launched the bundled host. Verified all 41 native/source file
hashes, all 306 entry hashes inside the three new source archives, and all 947 npm
notice/README hashes. The sole symlink is internal to the included src/js tree.
Uninstall removed the launcher/payload and preserved the initialized profile file.
[Installed result](install-summary.json). The user's Desktop was not changed.

The three source archives preserve all src/js entries, all six SQLite/Bun binding
files, and Node compatibility headers 24.3.0 plus their source LICENSE, with Bun's
openssl/uv exclusions. Their individual provenance and transforms are recorded in
packaged inventories. This is distinct from the independent Node host 24.19.0.
Runtime product code did not change; prior live oracle evidence retains its exact
artifact identity and was not rerun merely for these notice additions.

## Remaining concrete source/notice audit

1. Bun src outside src/js and src/jsc/bindings/sqlite: a read-only marker scan of
   the exact upstream archive found 527 files (9735416 bytes), including 492
   src/jsc/bindings files. Marker matches are leads, not a complete attribution
   selection rule; collecting full relevant source trees would preserve unmarked
   material as well.
2. WebKit and nested native component per-file attribution beyond currently
   supplied root notices. All 23 default Bun dependency recipes have component
   records, but recipe coverage does not establish an emitted linker map or the
   completeness of each component's nested material.
3. Corresponding-source/relinking verification for the static Bun/WebKit portion.
   The available full Bun source download is not its external dependency closure.
4. Upstream packages with declared-license supplements still have explicit
   attribution-review limitations; no missing copyright holder/year was invented.

These are unresolved checks, not a production-release PASS. Signing credentials
are not supplied; builds remain dev-only. Windows/macOS native work is outside
this user's current goal. Raw external archives and inspection output stay in
/tmp/loginom-bun-source-audit; only pinned selected inputs and sanitized evidence
are versioned.
